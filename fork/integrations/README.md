# Fork Integrations

This directory documents fork-only integrations and external components that are intentionally managed outside the main repository tree.

- `ubuntu-server-skills.md`: Ubuntu Server operator skills sourced from the external `agent-skills` repository
