# Upstream Merge Notes

This directory stores local notes about merging `upstream/main` into this repository.

Use it for:

- merge conflict patterns that repeat over time
- commands that worked well during upstream syncs
- post-merge verification steps that catch regressions early
- local guardrails that should be rechecked after large upstream updates

Do not store secrets here.
